# Generated by makejinja

talos_vms = {
#% for item in nodes %#
  "#{ item.name }#" = {
    hostname    = "#{ item.name }#"
    ip_address  = "#{ item.address }#"
    #% if item.vm_id %#
    vm_id       = "#{ item.vm_id }#"
    #% endif %#
    #% if item.controller %#
    tags        = ["terraform", "talos", "k8s", "master"]
    #% else %#
    tags        = ["terraform", "talos", "k8s", "worker"]
    #% endif %#
  }
#% endfor %#
}
